const Container = ({ children }) => {
    return <div style={{ backgroundColor: "#ededed", display: "flex", justifyContent: "center", height: "100vh", flexWrap: "wrap", alignItems: "center" }}>{children}</div>;
};

export default Container;
